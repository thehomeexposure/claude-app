import { authMiddleware } from "@clerk/nextjs";

export default authMiddleware({
  publicRoutes: ["/", "/sign-in(.*)", "/sign-up(.*)", "/_not-found", "/api/webhooks(.*)"],
});

export const config = { matcher: ["/((?!_next|.*\\..*|favicon.ico).*)"] };

